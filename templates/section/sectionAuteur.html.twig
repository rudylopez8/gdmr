
    <!-- Section-->
    <section class="py-5">
        <div class="container px-4 px-lg-5 mt-5">

            <div class="row gx-12 gx-lg-5 row-cols-2 row-cols-md-3 row-cols-xl-12 justify-content-center">
                {% for auteur in auteurs %}	
                    <div class="col mb-5">
                        <div class="card h-100">
                        <!-- Product image-->
                        <img
                            class="card-img-top"
                            src="{{ asset('images/1.png') }}" 
                            alt="..."
                        />
                        <!-- Product details-->
                            <div class="card-body p-4">
                                <div class="text-center">
                                <!-- Product name-->
                                <h5 class="fw-bolder">{{ auteur.noms}} </h5>

                                <!-- Product price-->
                                {{auteur.prenoms }}....
                                </div>
                            </div>
                            <!-- Product actions-->
                            <div class="card-footer p-4 pt-0 border-top-0 bg-transparent">
                                <div class="text-center">
                                <a href="{{path('app_auteur_affichage', {'id': auteur.id})}}" class="btn btn-primary"> Lire la suite </a> 

                                </div>
                            </div>
                        </div>
                    </div>  
                {% endfor %}
                
                </div>
        </div>

        
    </section>