{% extends 'base.html.twig' %}


{% block body %}

        {% include("menus/navigation.html.twig") %}
        {% include("section/section.html.twig") %}

        {% include("headers/header.html.twig") %}
    <div>
        
        <div class="container">
        
            <h1> ARTICLES </h1>

            {% for article in articles %}

                Titre:  {{ article.titre }} <br>
                    Auteur :{{ article.auteur }} <br>
                    
                    Resumé: {{ article.resume }} <br><br><br>
                    
                    {{ article.contenu }} <br><br><br>
                    Categorie: {{ article.Categorie.Titre }} <br>
                                <a href="{{path('app_article_affichage', {'id': article.id})}}" class="btn btn-primary"> Lire </a> 

            {% endfor %}


    </div> 
    </div>
{% endblock %}

{% block footer %} 
{% endblock%}